@model PostViewModel

<div>
    <div>
        <a asp-action="New" class="btn btn-primary">post img</a>
    </div>

    @foreach (Post post in Model.Posts) {
        @await Html.PartialAsync("PostCard", post)
    }
</div>
